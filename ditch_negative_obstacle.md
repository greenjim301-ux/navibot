# 沟(负障碍)风险 — 分层方案 + 层 2 的可行性验证

状态: **层 1 已实测并落地(`69c753a` / `760f472`), 层 2 已验证为"不能复用
`build_elevation`"(第 3 节)、且重写方案的关键假设也已实测(第 5 节, 结论不乐观),
层 0 一直可用, 层 4 没动。没有写任何新功能代码。**

起因是实机反馈: `618c099` 加了"全局规划优先走建图轨迹"之后,
**建图的时候人也会贴着沟沿走**, 于是导航时狗也会被带到沟边上, 有掉下去的风险。

相关文档: README「全局规划优先走建图轨迹」一节(层 1 的实测数据在那儿)、
`lidar_tilt_impact.md`(层 4)。

---

## 0. 问题的本质

`detect_structure`(`map_pipeline/elevation.py`)判障碍的判据只有一句话:
**这一格在「局部地面 + 0.10 ~ 0.55m」这个带子里有没有实体**。

沟是**负障碍**, 那个带子里什么都没有 → 判成 free。**沟压根不在地图里**, 所以:

> 这不是"膨胀不够"的问题。膨胀再大也膨胀不出一个不存在的障碍。

而轨迹记录的是**一个点**走过哪儿, 不是**一个机体**走过哪儿:

```
狗的碰撞包络(advanced_param.xml, double_cylinder 0.10/0.20)  0.6m 长 × 0.4m 宽 → 半宽 0.20m
五次曲线横向鼓包(0.8m 航点间距、45° 入口, 0.4·L·sinα)                        ≈ 0.23m
                                                          需要的单侧净空 ≈ 0.43m +
```

`save_map_small_1` 那条路线实测走廊净空**中位 0.54m、10 分位 0.41m** —— 即使在没有沟
的地方余量本来就已经很紧。人贴着沟沿走过一次, 翻译成狗就是机体外缘可能已经悬在沟上方
20~40cm。

---

## 1. 四层方案

| 层 | 做什么 | 状态 |
|---|---|---|
| **0** | 手工沿沟沿画禁行区 | ✅ 一直可用 |
| **1** | 关掉两个"拆安全余量"的开关 | ✅ 已实测, 收益很小 |
| **2** | 把沟自动标进地图 | ❌ **不能复用 `build_elevation`**, 见第 3 节 |
| **3** | 给"优先走轨迹"加置信度 | 未做, 依赖层 2 |
| **4** | 雷达前倾, 让局部规划器看得见地面 | 未做, 见 `lidar_tilt_impact.md` |

### 层 0 — 今天就能用

在 2D 图上沿沟沿画禁行区。链路是完整的:

- **全局规划**: 人工禁行区在 `free |= dilate(traj,1) & ~blocked` 的 `& ~blocked` 里
  **压过轨迹** —— "这里现在不许走"必须压过"历史上走过"这个事实
- **局部规划**: 经 `backend/app/virtual_obstacles.py` → hand-lio 注入 → 进 SCAN-Planner
  的 `grid_map`, 局部避障也看得见

`tools/gen_corridor_walls.py` 能沿轨迹自动生成走廊墙, `--half-width` 设到 0.4~0.5m
可以把狗夹在轨迹中间。代价是本来就窄的地方会规划不出来, 要逐图试。

**缺点是人工、不可扩展, 但这是唯一当天就能降风险的办法。**

### 层 1 — 已实测, 收益很小

详细数据在 README「为什么沟沿的安全余量不能靠关掉这两个开关拿回来」。结论:

- `clear_trajectory` **是承重墙关不得** —— 关掉后 `save_map_small_1` 上 47% 的轨迹格
  被膨胀吃掉, 轨迹上随机 12 对起终点 **0 对**能规划出来
- `BEATS_INFLATION` **关掉是免费的但基本没效果** —— `clear_trajectory` 已经在预处理
  阶段把轨迹旁边的障碍抹掉了, 实测轨迹落在膨胀带里的点是 **0/1795**, 它根本没东西可顶
- 已默认关掉 `BEATS_INFLATION`(去掉一个"必要时会压过安全余量"的机制), `clear_trajectory`
  维持 0.25(而且 0.1m/px 下 **0.25 已经是最小可用值**, 0.21~0.30 产出完全一样)

---

## 2. 层 2 原本的设想

`build_elevation`(`map_pipeline/elevation.py:219`)从轨迹出发**区域生长**地面面, 楼梯区
之外 `flat_step = 0.06m` 不允许跨过比这更大的高差。当时的想法是:

> 生长停下来的边界, 按定义就是"地面不再连续"的地方 —— 沟沿正好是它。

打算用三个信号:

| 信号 | 判据 |
|---|---|
| **a. 生长边界** | `elevation` 没覆盖、但 `detect_structure` 判 free 的格子 |
| **b. 高程突降** | 相邻格 `elevation` 差 < −0.15m → 把上沿那侧标成障碍 |
| **c. 地面缺失** | 有机体带以上的点、但没有地面回波 |

memory 里记的是"`build_elevation` 是现成的一半但从没跑过"。**这次跑了。**

---

## 3. 验证结果: 三个各自独立的拦路虎

### ① 大图跑不动

三维直方图是 `H × W × NZ × 4B`(`elevation.py:246`, 那行注释本身就警告过"对几十米
见方的大图会到 GB 级")。用**真实的 0.5~99.5 分位 z 跨度**算(不是全幅 z):

| 地图 | xy 跨度 | z 跨度 | 直方图 | 峰值(含 `uniform_filter` 两份同样大的副本) |
|---|---|---|---|---|
| `save_map_bedroom` | 8×6 m | 2.9 m | 2 MB | ✅ |
| `house` | 45×27 m | 15.2 m | 160 MB | ✅ |
| `save_map_stairs` | 51×47 m | 15.7 m | 319 MB | ✅ |
| `save_map_large_1` | 138×68 m | 32.5 m | 2.5 GB | ⚠️ **7 GB** |
| `save_map_small_1` | 190×234 m | 58.4 m | 20.8 GB | ❌ **62 GB** |

**而沟在室外大图上。** 能跑的三张全是室内小图。

> 这跟 `backend/app/path_planner.py` 模块 docstring 里"为什么不用点云"那段是同一个
> 原因 —— 当初就是因为"三维直方图能撑到 TB 级"才改成就近查建图轨迹的。

### ② 覆盖只是贴着轨迹的一条窄带 —— 而沟在带子外面

实跑三张能跑的(0.5~3 秒, 峰值内存 ≤0.6 GB):

| | 覆盖面积 | 占栅格 | 覆盖点离轨迹 中位 / 90分位 / **最大** |
|---|---|---|---|
| `save_map_bedroom` | 6.1 m² | 11.9% | 0.14 / 0.50 / **1.02 m** |
| `house` | 29.0 m² | 2.4% | 0.22 / 0.96 / **2.00 m** |
| `save_map_stairs` | 17.6 m² | 0.7% | 0.28 / 0.89 / **2.05 m** |

`house` 是 45×27m = 1215 m² 的图, 2D 栅格里可走面积 255 m², `build_elevation` 只认出
**29 m²(11%)**。而且**覆盖从来没超出轨迹 2m**, 离轨迹 >2.0m 的格子是 0~2 个。

**这直接毙掉信号 (a)**: 生长边界的位置主要由"**数据到此为止**"决定, 不是由"**地面不
连续**"决定。沟在路边 1~2m, 正好卡在这个边界上或外面, 两者分不开 —— 拿它当沟沿检测器
会满图误报。

### ③ 高程突降信号极稀疏, 而且是设计使然

覆盖区内相邻格 `|Δz| > 0.15m` 的边:

```
save_map_bedroom   20 / 1047     中位高差 0.000m
house              18 / 4946     中位高差 0.000m
save_map_stairs    18 / 2982     中位高差 0.000m   ← 这张图里有一整段真楼梯
```

连真有楼梯的图上也只有 18 条边超过 0.15m。原因是生长算法**本来就设计成能平滑跟上
台阶**(`ElevationParams.max_step = 0.25`、`stair_radius = 0.6`、`stair_slope = 0.30`)。

**拿它找沟等于要它做一件跟设计目标相反的事**, 不是调参能解决的。

### 但精度本身很好 —— 机制没坏, 只是范围不对

```
轨迹处高程误差(vs 轨迹 z − 传感器高):
  覆盖   163/163、523/523、260/260   —— 轨迹上 100% 有值
  中位   +0.009 / −0.004 / +0.001 m
  90分位 |误差| 0.051 / 0.044 / 0.063 m
  最大   0.300 / 0.152 / 0.144 m
```

`overlaps`(多层重叠)几乎不触发: bedroom 0 处、house 0 处、stairs 1 处。
`dropped_uncorroborated`(证据不足被丢掉的格子): 0 / 133 / 490。

---

## 4. 阶段结论: 层 2 要重写(但见第 5 节)

**`build_elevation` 不是"现成的一半"。** 它是为"给轨迹上的点查高程"设计的 —— 从轨迹
区域生长、遇到不确定就停、能平滑跟上台阶。找沟需要的**恰好相反**:

| | `build_elevation` | 找沟需要的 |
|---|---|---|
| 范围 | 轨迹两侧 ±0.5m 的带子 | **整片可走区域**(沟在路边) |
| 锚点 | 从轨迹生长 | 逐格独立, 不以轨迹为锚 |
| 遇到突变 | 平滑跟上(台阶) | **标出来**(沟沿) |
| 数据结构 | 3D 直方图 `H×W×NZ` | 2.5D 逐格统计 `H×W` |

好消息是新算法**便宜得多**: 不需要三维直方图, 只要对每格统计"合理带子里最低的一批
稠密回波", 内存是 `H×W` 而不是 `H×W×NZ` —— `save_map_small_1` 上从 **20.8 GB 降到
约 18 MB**。所以第 3 节的拦路虎 ① 在新写法下自动消失。

信号 (b)(相邻格高程突降)和 (c)(有上方回波但没有地面回波)在新的逐格高程面上都还
成立, 只有 (a)(生长边界)是 `build_elevation` 特有的、要放弃。

**建议**: 层 2 改成"新写一个 2.5D 地面提取", 而不是"复用 `build_elevation`"。
在那之前层 0(手工画禁行区)仍然是唯一能今天用的办法。

**但这个方案的关键假设也验过了, 结果不乐观 —— 见下一节。**

---

## 5. 2.5D 方案本身的验证: 卡点不是"沟 vs 空洞", 是"沟 vs 一切长得像沟的东西"

原本第 6 节里写的是"下一个要验的是能不能把沟造成的空洞和遮挡造成的空洞区分开,
很可能是最难的一步"。**这条猜错了。** 用一次性脚本在三张真图上实跑之后:

### ① 成本假设成立

逐格"最低的有支撑簇"(排序后 `searchsorted` 找同格内 `[z, z+0.20]` 里点数 >= 3 的
最低那个 z), 纯 numpy:

| 地图 | 点数 | 用时 | 峰值内存 |
|---|---|---|---|
| `house` | 1.70 M | 0.7s | 0.6 GB |
| `save_map_large_1` | 1.25 M | 0.5s | 0.5 GB |
| `save_map_small_1` | 4.52 M | **2.8s** | **1.1 GB** |

对比 `build_elevation` 在 `save_map_small_1` 上要 62 GB。**第 3 节的拦路虎 ① 确实消失了。**

### ② 但格子必须粗到 0.3m, 而且不能用分位数 —— 因为地面被天花板淹没了

第一版用"格内 z 的 5% 分位"当地面, 校验直接不通过(`house` 上"轨迹 z − 提取地面"
中位 **−0.335m**, 地面跑到传感器上方去了)。查了一个轨迹格:

```
轨迹点 (0.06, 1.56) z=0.01, 该 0.1m 格内 74 个点
   z: min 0.24   5% 1.62   25% 2.03   中位 2.07   max 2.16
                                       ^^^^^^^^ 天花板
```

**格内点的绝大多数是天花板, 地面只占最低的百分之一二。** 原因就是
`lidar_tilt_impact.md` 第 2 节那件事: Mid-360 视场下沿 −7°, 近处地面在盲锥里, 地面只能
从几米外掠射着扫到 —— 这是用另一份独立数据印证了那个分析。

改成"最低的有支撑簇"、并把格子从 0.1m 放粗到 0.3m 之后才收敛:

| res | `save_map_small_1` 轨迹处覆盖 / (轨迹z−地面) 标准差 | `house` 中位 |
|---|---|---|
| 0.10 | 534/793 / 2.061 | −0.293 ❌ |
| 0.20 | 788/793 / 0.351 | 0.277 |
| **0.30** | **793/793 / 0.140** ✅ | **0.553**(实测传感器高 0.545, 差 8mm)✅ |
| 0.50 | 793/793 / 0.146 | 0.567 |

**0.3m 格对找沟够用**(狗宽 0.4m), 但这条得写进任何实现里 —— 0.1m 是不行的。

### ③ 空洞不是问题 —— 在关心的范围内几乎不存在

沿轨迹向两侧每 0.3m 采一次, 把每个采样点分成四类:

`save_map_small_1`:

| 离轨迹 | 正常地面 | **下沉>0.3m** | 有点但取不到地面 | 完全没点 |
|---|---|---|---|---|
| 0.6m | 97.7% | 0.4% | 0.7% | 1.2% |
| 1.2m | 95.1% | 4.1% | 0.4% | 0.3% |
| 2.1m | 84.5% | 14.6% | 0.5% | 0.4% |
| 3.0m | 77.9% | 18.3% | 2.0% | 1.8% |
| 4.8m | 64.7% | 13.9% | 12.3% | 9.1% |

**3m 以内 ≥96% 的侧向采样都能取到地面**, "完全没点"只有 0~1.8%。空洞要到 3.5m 之外
才明显, 而且是**随距离单调增长**的 —— 那是覆盖随距离衰减, 本身就是个可用的判别特征。
`save_map_large_1` 形状一样。

而且"取不到地面"里 45~49% 是**完全没点**, 跟"有点但凑不出支撑簇"可以分开。

> **所以沟主要表现为"测得到的下沉", 不是空洞。** 信号 (b) 有数据支撑, 信号 (c) 基本用不上。

### ④ 真正的卡点: 下沉信号的**特异性**太差

把侧向剖面改成逐步(每 0.3m 一步)看高差, 0~3m 以内:

| 地图 | 单步下沉 >0.15m (>27°) | >0.25m (>40°) | >0.40m (>53°) |
|---|---|---|---|
| `save_map_small_1`(室外盘山路) | 6.80% | 3.01% | 1.70% |
| `save_map_large_1`(室外) | 8.84% | 4.82% | 3.05% |
| **`house`(平坦室内, 没有沟)** | **16.03%** | **12.34%** | **7.84%** |

**没有沟的那张图, 陡降率反而最高。** 那 12% 是家具边缘、桌面→地板的跃变、以及覆盖
边界上"最低有支撑簇"跳到另一个面造成的假阳性。

一条 3m 的侧向剖面有 10 步, 单步 12% 的误报率意味着**几乎每个轨迹点的两侧都会报出
至少一处"沟"**。这个信号不能直接用。

### 结论

- 原来担心的"沟 vs 空洞"**不是主要矛盾**(空洞在 3m 内几乎不存在)
- 真正的矛盾是"**沟 vs 一切长得像沟的东西**" —— 家具边、台沿、采样跃变
- **而且手上没有一张含已知沟的图, 没法测真阳率、也没法调阈值。** 现在只能测出
  假阳基线(平坦室内 12%), 测不出它能不能把真沟从里面挑出来

**下一步不是写代码, 是先建一张"故意沿着有沟的路走"的图。** 在那之前, 任何检测器
都只能对着 12% 的假阳基线调参, 调不出所以然。

---

## 6. 本文没做的事 / 已知不确定

- **全部是离线测量, 没有实机验证**, 也没有在真有沟的地图上验过 —— 手上这几张图里
  `save_map_stairs` 的楼梯是最接近"高程突变"的东西, 但楼梯和沟对算法来说不是一回事
  (一个要跟上、一个要标出来)。
- 第 3 节 ① 的内存数字是**按公式算的**, 没有真的去跑 `save_map_small_1` 撞 OOM。
- 第 5 节的 2.5D 提取是**一次性验证脚本**(在 scratchpad 里跑的), **没有进仓库**,
  也没有做任何工程化(边界处理、多层、与 2D 栅格图对齐都没管)。
- 第 5 节 ④ 的假阳率是"单步下沉超过阈值"的**裸比例**, 没有做任何后处理(连通性、
  沿轨迹方向的一致性、最小长度过滤)。真做检测器时这些都能把误报压下去 —— 但**压到
  多少、会不会连真沟一起压掉, 没有含沟的图就测不出来**。
- `lidar_tilt_impact.md` 第 2.1 节指出水平安装时机体高度带在狗周围 3.18m 内全在盲锥
  里 —— 也就是说**近处的地面在建图点云里本来就采样很差**, 这会直接影响 2.5D 地面提取
  的可靠性。两件事要一起看。
