# 雷达倾斜安装(30°)对全链条的影响 — 调研笔记

状态: **纯调研, 没有改任何代码。** 建图组件是第三方的、不感知倾斜, 我们只能消费
它的产物。

**2026-09-22 更新: 两个前提都变了 ——**

1. **重力对齐已由第三方确认**(原来只是本文第 1 节的间接推断)。
2. **倾角从 15° 改成约 30°**(前倾/低头)。

**一句话结论: 几何链条整条不用动 —— 重力对齐坐实之后这一点是确定的, 不再是"如果"。
30° 的代价全在视场、外参和重定位上, 而且多出三件 15° 时不存在的质变。**

> **本文按"前倾(低头)30°"算。倾斜方向决定一切结论** —— 后仰或侧倾的话所有数字都
> 要重算(后仰会把地面盲区推到身后, 结论几乎反过来)。装好之后先确认方向。

对照的源码:

- `/home/lisi/Documents/work/ros1/src/SCAN-Planner`
  (下面 `fsm.cpp` = `src/planner/plan_manage/src/scan_replan_fsm.cpp`)
- `/home/lisi/Documents/work/ros1/src/hand-lio`(`config/hand_lio.yaml`)
- 本项目: `map_pipeline/elevation.py`、`backend/app/global_planner.py`、
  `backend/app/path_planner.py`

---

## 1. 前提: 地图是重力对齐的 — 已确认

整份分析原本挂在这一条上: 世界系 z 轴对齐重力的话, 倾斜被吸收进初始姿态, 地图里
地面还是 `z = const`, 所有"z 朝上"的假设继续成立; 不对齐的话整张图歪 30°, 那是
灾难(见第 7 节)。

**第三方已经确认做了重力对齐**, 所以这一支定了。下面的离线证据现在只是旁证, 留着
是因为**换了新的建图组件/版本时可以用同一把尺子再量一遍**。

对现有地图的地面做最小二乘平面拟合(取轨迹附近 ±1m、轨迹高度下方 0.5~1.4m 的点
当地面):

| 地图 | 地面点数 | 拟合倾角 | 残差 std | 是否可信 |
|---|---|---|---|---|
| `save_map_bedroom` | 22193 | **0.17°** | 0.029m | ✅ 平坦室内 |
| `house` | 31353 | **0.31°** | 0.073m | ✅ 平坦室内 |
| `save_map_stairs` | 69653 | 16.5° | 0.224m | ❌ 拟合到楼梯斜面了 |
| `save_map_large_1` | 65418 | 15.2° | 0.777m | ❌ 室外坡地 |

后两张的残差是前两张的 3~27 倍,说明单平面模型根本不适用(图里本来就有坡),
那个 15~16° 是坡度不是安装倾角,不要拿来当证据。

**两张平坦室内图都在 0.3° 以内。** 建图设备是"手持设备绑在狗背上"
(`hand_lio.yaml:49` 的原话),每次开机的姿态不可能都一样 —— 如果世界系就是开机
时的传感器系,不可能两张图都这么平。所以判定: **第三方组件做了重力对齐。**

### 换组件/版本之后怎么再验一次

这套离线证据本身是**间接的**(不能完全排除"两次恰好都水平放置"), 之所以现在还能
下结论是因为第三方给了确认。**换了建图组件或版本就得重验**, 办法很轻:

1. 故意把设备明显倾斜着建一张小图(平坦室内即可, 走两圈就行)
2. 跑一遍下面那个平面拟合

出 0.x° → 重力对齐, 本文第 2~6 节成立。出 30° → 走第 7 节, 要大改。

拟合方法(上表就是这么跑出来的,换新图直接改 `m` 即可,需要 `schroot -c focal`):

```python
# 取轨迹上若干点, 各自在 ±1m 内、轨迹高度下方 0.5~1.4m 的点当地面样本,
# 合起来最小二乘拟一个平面 z = a*x + b*y + c, 法向跟 z 轴的夹角就是倾角。
# 残差 std 是这个拟合可不可信的判据 —— 大于 ~0.1m 说明图里本来就有坡,
# 单平面模型不适用, 算出来的角度没有意义(见上表后两行)。
raw = open('web_assets/map/%s/pointcloud.bin' % m, 'rb').read()
n   = struct.unpack_from('<I', raw, 4)[0]
P   = np.frombuffer(raw, dtype='<f4', count=n*3, offset=8).reshape(n, 3).astype(np.float64)
t   = path_planner.mapping_trajectory(m)
G = np.vstack([sel for p in t[::max(1, len(t)//60)]
               for sel in [P[(np.abs(P[:,0]-p[0])<1.0) & (np.abs(P[:,1]-p[1])<1.0)]]
               for sel in [sel[(sel[:,2] > p[2]-1.4) & (sel[:,2] < p[2]-0.5)]] if len(sel) > 20])
A = np.column_stack([G[:,0], G[:,1], np.ones(len(G))])
coef, *_ = np.linalg.lstsq(A, G[:,2], rcond=None)
nrm = np.array([-coef[0], -coef[1], 1.0]); nrm /= np.linalg.norm(nrm)
print(math.degrees(math.acos(abs(nrm[2]))), (G[:,2] - A @ coef).std())
```

---

## 2. 主要影响: 视场(FOV)

Mid-360 的垂直视场是 **−7° ~ +52°**(不对称, 主要朝上), 水平 360°。前倾 θ 就是把
这个锥绕机体 y 轴转 θ。

`detect_structure`(`map_pipeline/elevation.py:531`)判障碍用的"机体区间"是**局部
地面之上 0.10~0.55m**(`--map2d-body-clearance` / `--map2d-body-height`,
`generate_map_assets.py:640`/`:644`)。按传感器离地 0.94m 那一档换算, 机体区间相对
传感器是**下方 0.39~0.84m**。

| | 水平 | 15° | **30°** |
|---|---|---|---|
| 正前地面盲区半径 | 7.66 m | 2.33 m | **1.25 m** |
| 机体带**完全看不见** | < 3.18 m | < 0.97 m | **< 0.52 m** |
| 机体带**完整可见** | > 6.84 m | > 2.08 m | **> 1.11 m** |
| 1m 处天花板覆盖(离地) | 2.22 m | 1.69 m | **1.34 m** |
| 看到 2.0m 门框的最小距离 | 0.83 m | 1.41 m | **2.62 m** |

**天花板那两行没有看上去那么要紧**: `--map2d-body-height 0.55` 本来就把局部地面
0.55m 以上的点全当"狗能走过去"丢掉了, 所以上沿损失**不影响避障判据**。它影响的是
**LIO 的特征量和重定位**(第 4 节), 那才是代价所在。

### 2.1 对负障碍(沟)是实打实的改善

沟对 `detect_structure` 的机体高度带和 planner 的 ESDF 都不可见, 没有任何梯度能把
曲线拉回来(见 README「途经点间距」一节的警告)。30° 下**正前方 1.25m 外地面就可见**,
沟沿会产生明显的距离跳变。

`ditch_negative_obstacle.md` 里卡住的**层 2(用 `build_elevation` 把沟标进地图)第一次
有了数据基础** —— 那条线之前卡在"没有一张含真沟的图"上。这是倾斜最大的收益。

### 2.2 点密度分布会变, 而且比 15° 更剧烈

垂直面(墙、桌腿、路缘)靠接近水平的光束打才密。前倾之后入射角变陡, 同样距离上单位
面积的回波变少。

`detect_structure` 的 `min_support` 是从点密度的 75 分位现算的(`elevation.py` 里
`typical_density` 那段), 会自适应总体密度; 但 30° 下**近场密度暴涨**(0.52m 外机体
带就可见了), 自适应出来的阈值会被近场拉高。预期现象:

- **远处的低矮障碍更容易漏检**(比 15° 时更明显)
- 近处更容易误检

只能等实测点云回来再调 `--map2d-structure-min-support-frac` 和机体区间。

### 2.3 【30° 新增】上下楼梯时倾角和机体俯仰直接相加

两者都绕机体 y 轴, 所以是简单加减。按机体俯仰 ±25° 估:

| 场景 | 总倾角 | 世界系 FOV | 后果 |
|---|---|---|---|
| 平地 | +30° | `[−37°, +22°]` | 正常 |
| 上楼梯(抬头 25°) | +5° | `[−12°, +47°]` | 地面盲区涨到 **4.42 m** |
| 下楼梯(低头 25°) | +55° | `[−62°, `**`−3°`**`]` | **整个视场在水平面以下** |

**下楼梯这一条是质变**: 上沿都到了水平面以下, 下楼梯全过程中**前方与狗等高或更高
的障碍完全不可见**, 只能靠进入楼梯之前 grid_map 里留下的历史。15° 时是
`[−47°, +12°]`, 还留着 12° 余量。**这不是调参数能解决的, 是策略问题** —— 限速、
依赖历史地图、或者接受, 三选一。

上楼梯那一条反而是 30° 优于 15°: 15° 抬头之后总倾角 −10°, 下沿 +3° 朝上, **完全
看不到地面**; 30° 至少还有 4.42m。但"平地看得见脚下、上台阶反而看不见台阶"这个
反直觉的落差要记住。

### 2.4 【30° 新增】前后不对称, 后半球彻底看不到地面

倾斜是绕**机体 y 轴**而不是绕竖直轴, 所以那个 1.25m 只有正前方享受得到:

| 方位 | 世界系仰角 | 地面盲区半径 |
|---|---|---|
| 正前 | `[−37.0°, +22.0°]` | 1.25 m |
| 斜前 45° | `[−27.2°, +27.7°]` | 1.83 m |
| 正侧 | `[−6.1°, +43.0°]` | 8.86 m |
| 斜后 135° | `[+14.2°, +64.2°]` | **看不到地面** |
| 正后 | `[+23.0°, +82.0°]` | **看不到地面** |

(15° 时正后也看不到地面, 但斜后还能看到; 30° 连斜后 135° 都不行。)

这直接放大第 5 节的 grid_map 鬼影。**原地转身能恢复, 直线倒退不能。**

---

## 3. `lidar_R_body` 外参 — 30° 下是硬门槛

`~/Documents/work/ros1/src/hand-lio/config/hand_lio.yaml:48-57` 现在还是占位单位阵
+ 零向量。倾斜会直接进到这个没标定的外参里:

- **位置(杆臂效应)**: 按 0.3m 杆臂算, 竖直分量误差从 15° 的 0.078m 涨到 **0.150 m**。
- **姿态**: `/hand_lio/odom_vehicle` 会带 30° 的俯仰误差。
- **航点 yaw**: **纯 pitch 安装 + 平地时 yaw 恰好不受污染** —— `R = Rz(ψ)·Ry(θ)`
  的 ZYX 分解出来 yaw 就是 ψ(实算过)。但**机体一旦带横滚**(坡地、楼梯侧偏),
  乘积的 yaw 就不再是 ψ:

  | 机体横滚 | 安装 15° | 安装 **30°** |
  |---|---|---|
  | 2° | +0.54° | **+1.15°** |
  | 5° | +1.34° | **+2.88°** |
  | 10° | +2.66° | **+5.73°** |
  | 15° | +3.97° | **+8.50°** |

  而 `submit_route` 每个航点都带 yaw。

15° 时这条写的是"最好标一下", **30° 是硬门槛**。

### 怎么标(2026-09-21 补)

`tools/calibrate_cmd_vel.py` 现在能把这个外参顺带标出来,不用单独做一套流程 ——
标 yaw 轴时加一段**原地转两圈**(`--spin-turns`,默认开),报告里直接给出可以抄进
`hand_lio.yaml` 的 3x3。原理和验证见 README「/cmd_vel 标定 › 顺带能标 hand_lio 的
两个外参」,这里只记结论:

- **倾角**靠"雷达 z 轴在世界系里的水平投影随 yaw 画圆"分离 —— 圆心是地面坡度
  (固定在世界系),半径是安装倾角(固定在机体系)。**不能用 roll/pitch 欧拉角**,
  那里面这两样是搅在一起的。前提是第 1 节那条重力对齐结论成立。
- **绕 z 的偏转 γ** 靠 x/y 直线段的行进方向偏差,正反两个方向做两参数拟合,把
  "底盘自己往一边蹭"从 γ 里剥出来。
- **天花板: 安装倾角和狗自己的站姿倾角分不开**(都固定在机体系、都随 yaw 转,
  odom 只看得见雷达)。真要拆开得在机体上放水平仪或者量支架 —— 一次性机械测量,
  不是标定能解决的。所以报告给的是**建议值**。

> 注意标定脚本估倾角的原理**依赖重力对齐**(拿"雷达 z 轴在世界系的水平投影随 yaw
> 画圆"分离地面坡度和安装倾角)。这条前提现在已确认, 方法可用。

---

## 4. 已有地图必须重建

hand-lio 是拿实时扫描去跟先验地图做重定位的。地图是水平安装建的、运行时换成 30°
倾斜, 两边看到的场景结构**几乎不重叠** —— 见 2.4 那张表, 后半球从"能看见地面"变成
"只看天花板"。匹配上的对应点锐减。

后果沿链条传下去:

```
重定位变差
  → pose_cov 上升, 超过 hand_lio.yaml 的 pose_cov_reject_thresh: 0.99
  → path_planner._load_trajectory 过滤掉这些帧(_POSE_COV_COL)
  → route_manager.on_pose 跳过, 前端轨迹断线
  → 界面显示定位失败
```

15° 时这条写的是"**建议**重建"。**30° 是必须** —— 混用倾斜前后的图大概率直接重定位
失败, 而不只是精度下降。

---

## 5. SCAN-Planner 运行时 grid_map 会留下"鬼影"

grid_map 的体素衰减需要**真实光束穿过**才会掉(`p_miss = −0.8473`, 从饱和降到 free
要 3 个周期的有效穿透), `updateSlidingMap` 只负责清理滑出窗口的体素。

倾斜之后, 落在新视场之外的那一圈**再也收不到光束, 饱和的 occupied 体素就一直留着**。
表现是 planner 莫名其妙绕开一片空地。滑动窗口最终会清掉, 但在窗口内会持续存在。

**30° 下这一条被 2.4 的前后不对称放大**: 后半球(斜后 135° 起)完全收不到地面光束,
那一片的鬼影只能等滑窗。原地转身能恢复, 直线倒退不能。

---

## 6. 逐段过链条

重力对齐已确认, 所以下面这些判定是**确定的**, 不再是"如果第 1 节成立":

| 环节 | 受影响 | 说明 |
|---|---|---|
| **预处理** `estimate_sensor_height`(`elevation.py:476`) | 几乎不 | 杆臂旋转带来的变化比 15° 大(0.3m 杆臂约 15cm), 但它每张图现测, 自适应 |
| `detect_structure`(`:531`)机体区间 | **间接** | 判据本身没问题(地图是平的), 但输入点云的密度分布变了, 见 2.2 |
| `clear_trajectory` / `mark_known_region`(`:765`) | 不 | 纯 2D 半径, 跟 z 无关 |
| 2D 栅格投影 / `robust_xy_bounds` | 不 | 地图重力对齐, 投影面就是真水平面 |
| **全局规划** A\* / 膨胀 / 代价 | 不 | 纯 2D |
| `ground_elevation`(`path_planner.py:112`) / `_climb_ok`(`global_planner.py:478`) | 不 | 轨迹 z 仍是真实高度 |
| `_resample_polyline`(`global_planner.py:626`) | 不 | 纯弧长几何 |
| **局部规划** ESDF / B 样条优化 | **是** | 输入点云的覆盖变了, 见第 2、5 节 |
| `grad_3D.row(2).setZero()` | 不 | 本来就不优化 z |
| self_inflation 圆柱 | 不 | 世界系, 跟雷达朝向无关 |
| **执行** `z = ground + Δ` | 不 | Δ 的推导里常数 C 照样抵消(见 `path_planner.py` 模块 docstring) |
| 航点 yaw | **是** | 见第 3 节: 平地不受影响, 机体带横滚时 30° 下 5° 横滚就偏 2.88° |
| 到达判定 / 距离 | 不 | 旋转是等距变换, 距离不变 |
| 虚拟障碍注入(hand-lio) | 不 | 背面剔除用的是传感器**位置**不是朝向 |

---

## 7. 【已排除】万一不是重力对齐

第三方已确认重力对齐, **这一支不会发生**。保留摘要只为说明这个前提为什么关键、
以及换建图组件时该重新验什么:

整张图歪 30° 的话, `ground_elevation` 误差随离轨迹距离线性涨 **0.58 m/m**
(`tan30°`), 而机体区间只有 0.45m 宽, 离建图轨迹 0.8m 就整个脱离真实地面; 平地在
`GLOBAL_PLANNER_MAX_CLIMB_PER_SEGMENT_M = 0.20`(`config.py:102`)眼里成了持续爬坡,
单段被钉在 `0.20/tan30° = 0.35m`; 2D 投影有 13% 的各向异性尺度误差(`1−cos30°`);
200m 的图上出现 `200·tan30° = 115m` 的虚假高差。

**换了建图组件/版本就用第 1 节那段拟合代码重新验一次**, 十分钟的事。

---

## 8. 建议顺序

**必须做:**

1. **确认倾斜方向和实际角度**(本文按前倾 30° 算, 方向变了结论就变)。顺手看一眼
   近场点云, 确认 `blind: 0.35` 够不够 —— 按传感器离地 0.94m、机体顶 0.45m 算,
   最大俯角 37° 的光束打到机体高度是在水平 0.65m、斜距 0.81m 处, **不会打到自己**,
   但这依赖传感器具体装在哪, 纸上算不能打包票。
2. **标 `lidar_R_body` / `lidar_t_body`**(第 3 节)——
   `tools/calibrate_cmd_vel.py --axis yaw --axis x --axis y`。
3. **重建所有常用地图**(第 4 节)—— 30° 下这是必须不是建议。

**等实测点云:**

4. `--map2d-structure-min-support-frac` / `--map2d-body-clearance` /
   `--map2d-body-height`(2.2 节)。

**知道就行, 不用改代码:**

5. 下楼梯前向盲视(2.3 节)—— 限速 / 依赖历史地图 / 接受, 三选一。
6. 后半球鬼影(2.4 + 第 5 节)—— 滑窗会清, 窗口内持续存在。

---

## 9. 本文没做的事 / 已知不确定

- **全部是纸面几何 + 对现有地图的离线测量, 没有任何实机验证**(30° 安装本身还没
  装过)。
- Mid-360 的 −7°~+52° 取自官方规格书, 没有拿实际点云的仰角分布反算核对过。
  **30° 下这条比 15° 时更要紧** —— 上面所有新结论都是把这个区间转出来的。
- 视场数字按传感器离地 0.94m 算; 实测各图在 0.476~0.970m 之间浮动, 0.5m 那档要按
  同样方法重算(机体区间上沿会高过传感器, 近处反而部分可见)。
- 2.3 节的机体俯仰按 ±25° 估, **没有实测上下楼梯时的真实俯仰曲线** —— 那个角度直接
  决定"下楼梯瞎不瞎", 值得实测一次。
- 没有分析倾斜对第三方建图组件自身(回环、退化场景检测)的影响 —— 那是黑盒。
  **一个值得注意的方向**: 30° 下地面回波占比大幅上升, 而地面对平移是退化方向,
  特征贫乏区域(长走廊、空旷大厅)的定位可能变差。同样是黑盒, 只能实测。
