# 已通过 `python -m pip install ...` 装进运行环境, 这个文件只是记录依赖,
# 不需要再单独建 venv。
#
# **必须兼容 Python 3.8** —— 机器 (lubancat) 上跑的是 ROS Noetic 自带的 3.8,
# rospy 也是为它编译的。开发机用 mamba ros_host 里的 3.12, 写 dict[str,int]、
# X | None、asyncio.to_thread 都不报错, 推到机器上才在 import 阶段炸掉。
# 提交前跑一次: python3 tools/check_py38.py
fastapi
uvicorn[standard]
websockets
python-multipart
numpy          # 读 elevation.npy

# 以下由 ROS 环境提供, 不用 pip 装:
#   rospy, tf, geometry_msgs, nav_msgs, std_msgs, visualization_msgs
#
# 只有离线预处理 (map_pipeline/) 需要, 后端本身不依赖:
#   open3d, scipy, pillow
